Config = {
    commands = {
        openMenu = {
            enabled = true,
            name = 'adminmenu',
            help = 'Open the admin panel (Staff Only)',
            restricted = 'group.admin',
            keybind = {
                enabled = true,
                key = 'F9',
                help = 'Open the admin panel'
            }
        }
    },
}